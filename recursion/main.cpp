#include <iostream>

#include "fibonacci.h"
#include "reverse-number.h"

using namespace std;


int main() {
    int number = 0;
    int array[100];

    //cout << "Fibonacci (recursivo): " << fibonacci_recursive(number) << endl;
    //cout << "Fibonacci (iterativo): " << fibonacci_iterative(number) << endl;
    //cout << "Fibonacci (array): " << fibonacci_array(number) << endl;

    //cout << "Número invertido (iterativo): " << reverse_number_iterative(number) << endl;
    //cout << "Número invertido (recursivo): " << reverse_number_recursion(number) << endl;
    //cout << "Número invertido (log ln): " << reverse_number_ln(number) << endl;

    return 0;
}
